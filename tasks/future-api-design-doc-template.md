# Future task: API design document template / format

No GitHub issue filed. Raised by Jim in conversation, opening a new
mission, separate from the `claude-plugins`/`software-english` work in
progress.

## Ask

Jim, verbatim: "I spend a lot of time designing APIS I think we should
design a document template / format for API design. I will tell you
more about it. Create a task and we can start soecccing it out. One
part of it will be a format for presenting apps requests and
responses."

Read as: a document template/format for designing APIs generally
("apps requests and responses" read as a typo for API requests and
responses). One component identified so far: a format for presenting
API requests and responses within the document. The rest of the
template's scope is not yet defined; Jim said he would give more
detail.

## Status

Idea captured only. Not scoped, not designed, not started. Awaiting
further detail from Jim on the template's other parts before design
work begins.

## Open questions

- What other sections or parts does the template need, beyond the
  request/response format?
- Which repo this belongs in: a new document type inside
  `software-english` (alongside the existing RFC/Design types), a new
  plugin/skill in `claude-plugins`, or something else entirely.
